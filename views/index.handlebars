<!-- 5. Output the animalType string, pet boolean and fierceness number to the page for all the animals. -->
<ul>
  {{#each storyArr}}
  <li>

    <a href="{{link}}">{{title}}</a>

  </li>
  {{/each}}
</ul>